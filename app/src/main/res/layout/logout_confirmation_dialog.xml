<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center_horizontal">  <!-- Căn giữa tất cả các phần tử theo chiều ngang -->

    <!-- TextView hiển thị thông báo xác nhận đăng xuất -->
    <TextView
        android:id="@+id/tvConfirmLogout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bạn có chắc chắn muốn đăng xuất?"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:layout_marginBottom="20dp"
        android:gravity="center_horizontal"/>  <!-- Căn giữa text trong TextView -->

    <!-- LinearLayout chứa các nút Yes và No -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal">  <!-- Căn giữa các nút -->

        <!-- Nút Yes -->
        <Button
            android:id="@+id/btnYes"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Yes"
            android:layout_marginEnd="16dp"
            android:gravity="center"/>  <!-- Căn giữa nút Yes -->

        <!-- Nút No -->
        <Button
            android:id="@+id/btnNo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No"
            android:gravity="center"/>  <!-- Căn giữa nút No -->
    </LinearLayout>

</LinearLayout>
